<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hoowe.locationprovider">

    <application
        android:name=".application.LocationApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="填写你的Api_Key" />  <!--http://lbsyun.baidu.com/apiconsole/key-->

        <activity android:name=".ui.MainActivity" />

        <activity android:name=".ui.LocationActivity" />

        <activity android:name=".ui.LocationTrackActivity" />

        <activity android:name=".ui.PeriodLocationActivity" />

        <activity android:name=".ui.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.TimeLocationActivity" />

    </application>

</manifest>